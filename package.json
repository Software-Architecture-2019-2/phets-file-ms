{
  "name": "phets-file-manager-ms",
  "version": "1.0.0",
  "description": "Microservice to manage files. Specially images and video",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "microservice",
    "media"
  ],
  "author": "phets",
  "license": "ISC",
  "dependencies": {
    "@types/multer": "^1.3.10",
    "@types/uuid": "^3.4.5",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "typescript": "^3.6.3",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.12",
    "tslint": "^5.20.0"
  }
}
